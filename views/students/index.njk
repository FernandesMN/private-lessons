{% extends "layout.njk" %}

{% block head %}
    <title>Private Lessons - Students</title>
{% endblock %}

{% block content %}
    <div class="card table-students">
        <table width="100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Side dish</th>
                    <th>Action</th>
                </tr>
            </thead>
        
            <tbody>
                {% for student in students %}
                    <tr>
                        <td><span style="background-image: url({{student.avatar_url}})"></span>{{student.name}}</td>
                        <td>
                            {% for acting in student.acting %}
                                <span>{{acting}}</span>
                            {% endfor %}
                        </td>
                        <td><a href="/students/{{student.id}}">show</a></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

